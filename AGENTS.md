# AgentOS Operating Manual

> Auto-generated by `agentOS apply`. Edit this file to customise role definitions
> for your project. Do not change the state machine section — it is used by the
> orchestrator workflow.

## Architecture

GitHub labels drive a state machine across issues and pull requests. Each status
label transition fires the orchestrator workflow (.github/workflows/orchestrator.yml).
The workflow identifies the routing table entry for the new label, mints a
short-lived GitHub App installation token scoped to the repository, injects a
standard set of environment variables, and calls the runner command. The runner
starts the agent process, which reads the issue/PR context, does its work, and
posts a run receipt as a comment before exiting. There is no persistent agent
process — the system is entirely event-driven and stateless between runs.

## Roles

| Role | Triggered by | Can push code? | Notes |
|---|---|---|---|
| builder | status:todo, status:changes-requested | Yes | Power role |
| reviewer | status:in-review | No (intentional) | Cannot push |
| watcher | schedule / settlement | No | Minimal footprint |
| board | PR close | No | Projects v2 only |
| docs | status:approved (if enabled) | Yes | Reuses builder App |
| planner | status:planning (if enabled) | Yes | Reuses builder App |

## State Machine

```
status:planning         -> planner
status:todo             -> builder
status:in-progress      -> (informational)
status:in-review        -> reviewer
status:changes-requested -> builder
status:approved         -> docs (if enabled)
status:blocked          -> HUMAN
status:done             -> terminal
```

## Execution Protocol (Builder)

1. Read issue title, body, and linked PR (if any)
2. Understand what Done looks like before touching any file
3. Make the smallest change that satisfies the issue
4. Run tests / lint if the project has them
5. Open a PR from branch `agent/builder/{issue}-{slug}`
6. Apply `status:in-review` to the issue
7. Exit cleanly (exit 0)

## Execution Protocol (Reviewer)

1. Read the PR diff carefully
2. Check: scope (does it match the issue?), correctness, style
3. If approved: apply `status:approved` to the issue
4. If changes needed: apply `status:changes-requested` + `review:scope-violation` if relevant
5. Post a detailed review comment before changing the label
6. NEVER push commits to the branch

## Guardrail Rules

- No infinite label loops: do not re-apply the same status label you just removed
- If stuck after 2 retries: apply `status:blocked` and explain in a comment
- Max turns: respect AGENT_MAX_TURNS env var; set status:blocked before exiting if incomplete
- Secrets: never log or echo credential values; never commit .env or *.pem files
- Scope: only modify files relevant to the issue; do not refactor unrelated code

## Branch Convention

```
agent/{role}/{issue_number}-{slug}
```

Examples:
```
agent/builder/42-add-user-auth
agent/reviewer/42-add-user-auth
```

## Environment Variables Injected at Runtime

```
AGENT_ROLE          Role name
ISSUE_NUMBER        Issue number (integer string)
GITHUB_TOKEN        Short-lived App installation token
GITHUB_REPOSITORY   owner/repo
GITHUB_RUN_ID       Actions run ID
LLM_PROVIDER        Provider slug (if set)
LLM_MODEL           Model identifier (if set)
AGENT_MAX_TURNS     Turn budget (if set)
```

## Runner

```
cat agents/$AGENT_ROLE/AGENT.md | claude -p --dangerously-skip-permissions --output-format json --max-turns $AGENT_MAX_TURNS
```

Replace this with your actual runner command. The runner must:
- Accept the above env vars
- Exit 0 on clean completion, 1 on error, 2 on max-turns
- Post a run receipt comment before exiting (or call run-receipt.yml)

## Project-Specific Notes

### PMQs web layer — read before touching `app.html`

The PMQs app has **one** HTML template: `pmqs/pmqs/web/templates/app.html`.
`pmqs/pmqs/web/render.py` splices real data into it at request time using anchored
regular expressions matched against its markup.

This means the template's **class names, `id`s, `data-*` attributes, HTML comment
sentinels and DOM nesting depth are a load-bearing API**, not cosmetic. Renaming
`.card`, deleting the `<!-- WORKSPACE VIEW -->` comment, or adding one wrapper div
around the artifact pane will break rendering.

**The test suite does not catch this.** All 109 tests pass with the template's markup
arbitrarily broken — no test asserts on markup. Failures surface at request time, or as
a page that quietly renders fixture data instead of real data.

Before changing that file, read **`pmqs/pmqs/web/TEMPLATE-CONTRACT.md`**, which
enumerates every anchor. Colours, fonts, spacing and shadows are free to change;
structure and names are not. If you must change an anchor, change `render.py` in the
same commit and verify in a browser — `pytest` passing is necessary but not sufficient.

### Standing hazards

- Do **not** run `agentos apply` or `agentos upgrade` against this repo until the
  adopt→upgrade data-loss bug is fixed.
- `docs/build-spec-*.md` are historical phase records. They describe what was true at
  the time and are not maintained as current documentation. Do not "correct" them.
